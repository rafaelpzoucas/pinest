export type ExtraType = {
  created_at: string;
  id: string;
  name: string;
  price: number;
  quantity: number;
  store_id: string;
};

export type SelectedExtraType = {
  extra_id: string;
  name: string;
  price: number;
  quantity: number;
};
